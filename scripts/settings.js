$(document).ready(function() 
{
	/*	Use "true" to enable feature, "false" to disable feature	*/
	
		newwindow = false;


if(newwindow)
			html = html + '<li><a href="' + url + '" target="_blank">' + title + '</a></li>'
		else
			html = html + '<li><a href="' + url + '">' + title + '</a></li>'
});